//FOR IN RANGER, laço de repetição que irá percorrer de X a Y.

const numeros = [2, 4, 6, 8, 10, 12];

let i = 0;

while (i < 5) { //condição para que haja contagem
    console.log(numeros[i]);
    i += 1; // esta informação evita de criar um loop infinito
}