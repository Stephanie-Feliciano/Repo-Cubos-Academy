const array = [0, 1, 2, 3, 4];


// Lógica de como realizar a soma da array utilizando o FOR
// let somaTotal = array[0];

// for (i = 1; i < array.length; i++) {
//   const elementoAtual = array[i];

//   somaTotal = somaTotal + elementoAtual;
// };

// console.log(somaTotal);

const resultado = array.reduce((acumulador, elementoAtual, indice, array) => {
  return acumulador + elementoAtual;
});

console.log(resultado);

const novoResultado = array.reduce((acumulador, elementoAtual, indice, array) => {
  return acumulador + elementoAtual;
}, 25);

console.log(novoResultado);

